export interface ILayoutProps {
  children: React.ReactNode;
}

export interface INews {
  id: string;
  slug: string;
  title: string;
  content: string;
  image: string;
  date: string;
}
